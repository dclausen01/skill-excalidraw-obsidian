# `dreieck`-Helfer Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Ein `dreieck`-Helfer, der ein gleichseitiges Dreieck (Spitze oben) mit drei Ecken-Labels als geschlossene `line` baut, sowie eine optionale Füllung für das `line`-Primitiv.

**Architecture:** Das `line`-Primitiv (Teil 1) bekommt eine optionale `fuellung` (Hintergrundfarbe). `dreieck` (neue Datei `lib/shapes/dreieck.js`) komponiert eine geschlossene `line` mit drei `frame.text`-Ecken-Labels; die Labels werden nach dem Messen per Delta außerhalb der Ecken positioniert.

**Tech Stack:** Node ≥ 20 (ESM), vitest, bestehender Puppeteer-Renderer.

## Global Constraints

- **Node ≥ 20**, ausschließlich ESM.
- **Determinismus:** dieselbe Szene → byte-identische Ausgabe. Inhaltsbasierte ids/seeds; `fuellung` fließt in den `line`-id-Schlüssel.
- **Sprache:** deutsche Optionsschlüssel und Bezeichner (`ecken`, `breite`, `hoehe`, `fuellung`, `dreieck`); technische Exporte englisch wo etabliert.
- **Hausstil:** `FARBROLLEN` (`{ strich, fuellung }` je Rolle) und `STRICH` aus `lib/style.js`. Umriss-Standardrolle `neutral`, Label-Typo `kernbegriff`.
- **Geschlossene Form = Loopback** (Teil-1-Spike; `line` schließt via Wiederholung des ersten Punkts). Füllung wirkt nur bei `geschlossen: true`.
- **Bestehende `line`-Golden bleiben byte-identisch** (Füllung nur bei explizitem `fuellung`).

## Dateistruktur

| Datei | Verantwortung |
|---|---|
| `lib/elements.js` | **Änderung:** `lineElement` um `fuellung` (backgroundColor, id-Schlüssel) |
| `lib/scene.js` | **Änderung:** `f.line` reicht `fuellung` durch |
| `lib/shapes/dreieck.js` | **neu:** `dreieck(...)` |
| `lib/index.js` | **Änderung:** Export `dreieck` |
| `references/{builder-api,muster}.md` | **Änderung:** `dreieck` dokumentieren |
| `tests/…` | Unit-, Validator-, Golden-Tests |

---

### Task 1: `line`-Füllung

**Files:**
- Modify: `lib/elements.js`, `lib/scene.js`
- Test: `tests/line-fuellung.test.js`

**Interfaces:**
- Consumes: `FARBROLLEN` (`lib/style.js`).
- Produces: `lineElement({ punkte, rolle?, geschlossen?, strichbreite?, fuellung?, ordnung })` — mit `fuellung` wird `backgroundColor` gesetzt (Rolle → `FARBROLLEN[fuellung].fuellung`, sonst literale Farbe); ohne bleibt `"transparent"`. `f.line(punkte, { …, fuellung? })` reicht es durch.

- [ ] **Step 1: Test schreiben**

```js
// tests/line-fuellung.test.js
import { describe, it, expect } from "vitest";
import { scene } from "../lib/scene.js";
import { FARBROLLEN } from "../lib/style.js";

describe("line-Füllung", () => {
  it("setzt backgroundColor aus einer literalen Farbe", () => {
    const s = scene();
    const f = s.frame("K", { x: 0, y: 0 });
    const l = f.line([[0, 0], [100, 0], [50, 80]], { geschlossen: true, fuellung: "#b2f2bb" });
    expect(l.backgroundColor).toBe("#b2f2bb");
    expect(l.fillStyle).toBe("solid");
  });

  it("löst einen Rollennamen auf FARBROLLEN[rolle].fuellung auf", () => {
    const s = scene();
    const f = s.frame("K", { x: 0, y: 0 });
    const l = f.line([[0, 0], [100, 0], [50, 80]], { geschlossen: true, fuellung: "ergebnis" });
    expect(l.backgroundColor).toBe(FARBROLLEN.ergebnis.fuellung);
  });

  it("bleibt ohne fuellung transparent", () => {
    const s = scene();
    const f = s.frame("K", { x: 0, y: 0 });
    const l = f.line([[0, 0], [100, 0]]);
    expect(l.backgroundColor).toBe("transparent");
  });

  it("unterscheidet gefüllte und ungefüllte Linie deterministisch (verschiedene ids)", () => {
    const s = scene();
    const f = s.frame("K", { x: 0, y: 0 });
    const a = f.line([[0, 0], [100, 0], [50, 80]], { geschlossen: true });
    const b = f.line([[0, 0], [100, 0], [50, 80]], { geschlossen: true, fuellung: "#b2f2bb" });
    expect(a.id).not.toBe(b.id);
  });
});
```

- [ ] **Step 2: Test laufen lassen — muss fehlschlagen**

Run: `npx vitest run tests/line-fuellung.test.js`
Expected: FAIL — `backgroundColor` ist `"transparent"` statt der Füllfarbe.

- [ ] **Step 3: Implementieren**

In `lib/elements.js`, `lineElement` um `fuellung` erweitern. Signatur und die zwei betroffenen Zeilen:

```js
export function lineElement({ punkte, rolle = "kontext", geschlossen = false, strichbreite, fuellung, ordnung }) {
  // … unveränderte Punkt-Normalisierung …
  // WICHTIG: den fuellung-Teil NUR bei gesetzter Füllung anhängen — sonst ändert
  // sich die id (und damit seed/Rendering) jeder bestehenden ungefüllten Linie und
  // die Golden-PNGs (z. B. tabelle.png) brechen.
  const id = elementId(`line:${JSON.stringify(punkte)}:${geschlossen}${fuellung ? `:${fuellung}` : ""}`, ordnung);
  const backgroundColor = fuellung
    ? (FARBROLLEN[fuellung]?.fuellung ?? fuellung)
    : "transparent";
  return {
    // … unverändert bis backgroundColor …
    backgroundColor,          // statt der bisherigen Konstante "transparent"
    // … Rest unverändert …
  };
}
```

In `lib/scene.js`, `f.line` (bei `lineElement({ … })`) `fuellung: opts.fuellung` ergänzen:

```js
const l = lineElement({ punkte: absolut, rolle: opts.rolle, geschlossen: opts.geschlossen, strichbreite: opts.strichbreite, fuellung: opts.fuellung, ordnung: ordnung++ });
```

- [ ] **Step 4: Test laufen lassen — muss bestehen**

Run: `npx vitest run tests/line-fuellung.test.js`
Expected: PASS (4 Tests).

- [ ] **Step 5: Regression — bestehende line-Golden byte-identisch**

Run: `npx vitest run tests/line-element.test.js tests/golden-render.test.js`
Expected: PASS — ungefüllte Linien unverändert (`backgroundColor` weiterhin `"transparent"`), alle Golden byte-identisch.

- [ ] **Step 6: Commit**

```bash
git add lib/elements.js lib/scene.js tests/line-fuellung.test.js
git commit -m "feat: optionale Füllung für das line-Primitiv (fuellung -> backgroundColor)"
```

---

### Task 2: `dreieck`-Helfer

**Files:**
- Create: `lib/shapes/dreieck.js`
- Modify: `lib/index.js`
- Test: `tests/dreieck.test.js`

**Interfaces:**
- Consumes: `frame.line` (mit `geschlossen`/`fuellung` aus Task 1), `frame.text`.
- Produces: `dreieck(frame, [oben, untenLinks, untenRechts], { x = 0, y = 0, breite, hoehe?, fuellung?, rolle = "neutral", typo = "kernbegriff" }) → { dreieck: <line>, ecken: [<text|null>×3] }`.

**Geometrie/Labels:** Spitze `(x+breite/2, y)`, unten-links `(x, y+hoehe)`, unten-rechts `(x+breite, y+hoehe)`; `hoehe` Standard `breite·√3/2`. Labels werden provisorisch an der Ecke platziert, dann per Delta (aus gemessener `width`/`height`) nach außen justiert (`AUSSEN = 24`): oben mittig darüber, unten-links links daneben, unten-rechts rechts daneben.

- [ ] **Step 1: Test schreiben**

```js
// tests/dreieck.test.js
import { describe, it, expect } from "vitest";
import { scene } from "../lib/scene.js";
import { dreieck } from "../lib/shapes/dreieck.js";

function frame() {
  const s = scene();
  return { s, f: s.frame("K", { x: 0, y: 0 }) };
}

describe("dreieck", () => {
  it("baut eine geschlossene Linie mit Basisbreite und gleichseitiger Höhe", () => {
    const { f } = frame();
    const { dreieck: d } = dreieck(f, ["A", "B", "C"], { x: 100, y: 100, breite: 400 });
    expect(d.type).toBe("line");
    expect(d.width).toBeCloseTo(400, 5);
    expect(d.height).toBeCloseTo(400 * Math.sqrt(3) / 2, 3);
    expect(d.points).toHaveLength(4);                    // geschlossen (Loopback)
  });

  it("respektiert eine explizite hoehe", () => {
    const { f } = frame();
    const { dreieck: d } = dreieck(f, ["A", "B", "C"], { x: 0, y: 0, breite: 400, hoehe: 300 });
    expect(d.height).toBeCloseTo(300, 5);
  });

  it("platziert das obere Label mittig über der Spitze", () => {
    const { f } = frame();
    const { ecken } = dreieck(f, ["Spitze", "", ""], { x: 100, y: 100, breite: 400 });
    const oben = ecken[0];
    // Frame bei (0,0) → absolute = frame-relative. Spitze bei x=300, y=100.
    expect(oben.x + oben.width / 2).toBeCloseTo(300, 1);   // horizontal mittig
    expect(oben.y + oben.height).toBeCloseTo(100 - 24, 1); // knapp über der Spitze (AUSSEN=24)
  });

  it("gibt null für leere Ecken zurück", () => {
    const { f } = frame();
    const { ecken } = dreieck(f, ["A", "", "C"], { x: 0, y: 0, breite: 400 });
    expect(ecken[1]).toBe(null);
    expect(ecken[0].rawText).toBe("A");
    expect(ecken[2].rawText).toBe("C");
  });

  it("reicht die Füllung an die Linie durch", () => {
    const { f } = frame();
    const { dreieck: d } = dreieck(f, ["A", "B", "C"], { x: 0, y: 0, breite: 400, fuellung: "ergebnis" });
    expect(d.backgroundColor).not.toBe("transparent");
  });

  it("wirft bei falscher Eckenzahl oder fehlender breite", () => {
    const { f } = frame();
    expect(() => dreieck(f, ["A", "B"], { x: 0, y: 0, breite: 400 })).toThrow();
    expect(() => dreieck(f, ["A", "B", "C"], { x: 0, y: 0 })).toThrow();
  });
});
```

- [ ] **Step 2: Test laufen lassen — muss fehlschlagen**

Run: `npx vitest run tests/dreieck.test.js`
Expected: FAIL — `dreieck` nicht importierbar.

- [ ] **Step 3: Implementieren**

`lib/shapes/dreieck.js` neu:

```js
/**
 * Gleichseitiges Dreieck (Spitze oben) als geschlossene line mit drei
 * Ecken-Labels. ecken = [oben, unten-links, unten-rechts]; leerer String → kein
 * Label. Standard nur Umriss; mit fuellung dezent getönt. Labels sitzen außen an
 * den Ecken (AUSSEN Abstand), mittig zur jeweiligen Ecke.
 */
const AUSSEN = 24;

export function dreieck(frame, ecken, { x = 0, y = 0, breite, hoehe, fuellung, rolle = "neutral", typo = "kernbegriff" } = {}) {
  if (!Array.isArray(ecken) || ecken.length !== 3) throw new Error("dreieck braucht genau drei Ecken-Label (oben, unten-links, unten-rechts)");
  if (!breite) throw new Error("dreieck braucht eine breite");

  const h = hoehe ?? (breite * Math.sqrt(3)) / 2;
  const spitze = [x + breite / 2, y];
  const untenLinks = [x, y + h];
  const untenRechts = [x + breite, y + h];

  const linie = frame.line([spitze, untenLinks, untenRechts], { geschlossen: true, rolle, fuellung });

  const [obenText, ulText, urText] = ecken;
  const eckenEl = [null, null, null];

  if (obenText) {
    const l = frame.text(obenText, { typo, x: x + breite / 2, y });   // provisorisch an der Spitze
    l.x -= l.width / 2;                 // horizontal zentrieren
    l.y -= AUSSEN + l.height;           // über die Spitze
    eckenEl[0] = l;
  }
  if (ulText) {
    const l = frame.text(ulText, { typo, x, y: y + h });              // provisorisch an der linken Ecke
    l.x -= l.width;                     // links neben die Ecke
    l.y += AUSSEN;                      // unter die Basis
    eckenEl[1] = l;
  }
  if (urText) {
    const l = frame.text(urText, { typo, x: x + breite, y: y + h });  // rechte Ecke (kein x-Delta)
    l.y += AUSSEN;
    eckenEl[2] = l;
  }

  return { dreieck: linie, ecken: eckenEl };
}
```

In `lib/index.js` den Export ergänzen (eigene Zeile, da neue Datei):

```js
export { dreieck } from "./shapes/dreieck.js";
```

- [ ] **Step 4: Test laufen lassen — muss bestehen**

Run: `npx vitest run tests/dreieck.test.js`
Expected: PASS (6 Tests).

- [ ] **Step 5: Volle Suite**

Run: `npx vitest run`
Expected: PASS. (`tests/render.test.js` kann unter Last flackern — isoliert nachprüfen.)

- [ ] **Step 6: Commit**

```bash
git add lib/shapes/dreieck.js lib/index.js tests/dreieck.test.js
git commit -m "feat: dreieck-Helfer (gleichseitig, Spitze oben, Ecken-Labels)"
```

---

### Task 3: Validator-Test (Dreieck ohne Befund)

**Files:**
- Test: `tests/dreieck-validate.test.js`
- Modify: keine Produktionsdatei (nur Absicherung)

**Interfaces:**
- Consumes: `dreieck`, `sceneToMarkdown`, `validateScene`.
- Produces: sichert, dass ein vollständiges Dreieck mit Ecken-Labels **keinen** Validierungsbefund erzeugt (keine Falschwarnung durch die neue Form — dieselbe Eigenschaft, die schon `line`/`tabelle` absichern).

- [ ] **Step 1: Test schreiben**

```js
// tests/dreieck-validate.test.js
import { describe, it, expect } from "vitest";
import { scene } from "../lib/scene.js";
import { dreieck } from "../lib/shapes/dreieck.js";
import { sceneToMarkdown } from "../lib/document.js";
import { validateScene } from "../lib/validate/index.js";

describe("Dreieck validiert sauber", () => {
  it("erzeugt keinen Befund (neue Form → keine Falschwarnung)", () => {
    const s = scene();
    const f = s.frame("Gewaltdreieck");
    f.text("Das Gewaltdreieck nach Galtung", { typo: "frametitel", x: 60, y: 55 });
    dreieck(f, ["personelle Gewalt", "strukturelle Gewalt", "kulturelle Gewalt"],
      { x: 600, y: 300, breite: 700 });
    const md = sceneToMarkdown(s, { pluginVersion: "x" });
    const { findings } = validateScene(s.elements(), { markdown: md });
    expect(findings).toEqual([]);
  });
});
```

- [ ] **Step 2: Test laufen lassen**

Run: `npx vitest run tests/dreieck-validate.test.js`
Expected: PASS. Schlägt er fehl, ist eine Falschwarnung aufgetreten (z. B. Label ragt über den Frame, oder Überlappung) — dann die Label-Platzierung/Position im Golden-Task prüfen und `dreieck` anpassen, NICHT den Test aufweichen.

- [ ] **Step 3: Commit**

```bash
git add tests/dreieck-validate.test.js
git commit -m "test: Dreieck validiert ohne Befund"
```

---

### Task 4: Referenzen dokumentieren

**Files:**
- Modify: `references/builder-api.md`, `references/muster.md`
- Test: keine (Doku)

**Interfaces:**
- Consumes: `dreieck`-API aus Task 2.
- Produces: `dreieck` in den Skill-Referenzen; „Noch nicht baubar" um Dreiecke gekürzt.

- [ ] **Step 1: `builder-api.md` erweitern**

Im Abschnitt „Linien und Tabellen" (oder direkt danach) ergänzen:

````markdown
```js
// Dreieck (z. B. Gewaltdreieck): Spitze oben, drei Begriffe an den Ecken
dreieck(f, ["personelle Gewalt", "strukturelle Gewalt", "kulturelle Gewalt"],
  { x: 600, y: 300, breite: 700 });
// mit dezenter Tönung:
dreieck(f, ["These", "Antithese", "Synthese"], { x: 100, y: 200, breite: 500, fuellung: "ergebnis" });
```

- `dreieck(frame, [oben, untenLinks, untenRechts], { x, y, breite, hoehe?, fuellung?, rolle?, typo? })` — gleichseitiges Dreieck (Spitze oben) als geschlossene Linie; drei Ecken-Labels (leerer String = keins). `hoehe` Standard gleichseitig. `fuellung` (Farbe/Rolle) tönt dezent, sonst nur Umriss. Rückgabe `{ dreieck, ecken }`.
````

- [ ] **Step 2: `muster.md` aktualisieren**

Eine Zeile in der Frame-Ebene-Tabelle ergänzen:

```markdown
| **Dreieck / Gewaltdreieck** | `dreieck(frame, [oben, links, rechts], { breite })` | drei Pole/Begriffe an den Ecken (z. B. Galtung) |
```

Im Abschnitt „Noch nicht baubar" die Dreiecke streichen:

```markdown
Mengenkreise (Venn), Programmablaufpläne. Wenn ein Board das braucht, ehrlich sagen — nicht behelfsmäßig mit falschen Primitiven nachbauen.
```

- [ ] **Step 3: Gegencheck + Commit**

Run: `grep -rn -i "dreieck" references/muster.md references/builder-api.md | grep -i "nicht baubar" || echo "(sauber)"`
Expected: `(sauber)`.

```bash
git add references/builder-api.md references/muster.md
git commit -m "docs: dreieck in Skill-Referenzen"
```

---

### Task 5: Golden-Referenz (Umriss + gefüllt)

**Files:**
- Create: `tests/golden/dreieck.mjs`, `tests/golden/dreieck.png`, `tests/golden/dreieck-gefuellt.mjs`, `tests/golden/dreieck-gefuellt.png` (generiert)
- Test: `tests/golden-render.test.js` (bestehend, greift automatisch)

**Interfaces:**
- Consumes: `scene`, `dreieck`, der Renderer.
- Produces: zwei Golden-Referenzszenen (Gewaltdreieck nur Umriss; eine gefüllte Variante).

**Dieser Task gehört dem Controller** (wie die früheren Golden-Tasks): Szenen schreiben, `npm run update-golden`, **beide Bilder mit dem Read-Tool ansehen** und prüfen (Spitze oben, Labels außen/mittig an den Ecken, nichts berührt den Umriss, Füllung dezent), erst dann committen. Bei Mängeln `AUSSEN`/Label-Positionen bzw. die Füllfarbe anpassen und neu erzeugen.

- [ ] **Step 1: Referenzszenen schreiben**

```js
// tests/golden/dreieck.mjs — Gewaltdreieck, nur Umriss
import { scene } from "../../lib/scene.js";
import { dreieck } from "../../lib/shapes/dreieck.js";

const s = scene();
const f = s.frame("Gewaltdreieck");
f.text("Das Gewaltdreieck nach Galtung", { typo: "frametitel", x: 60, y: 55 });
dreieck(f, ["personelle Gewalt", "strukturelle Gewalt", "kulturelle Gewalt"],
  { x: 560, y: 320, breite: 800 });

export default s;
```

```js
// tests/golden/dreieck-gefuellt.mjs — gefüllte Variante
import { scene } from "../../lib/scene.js";
import { dreieck } from "../../lib/shapes/dreieck.js";

const s = scene();
const f = s.frame("These-Antithese-Synthese");
dreieck(f, ["These", "Antithese", "Synthese"], { x: 600, y: 300, breite: 700, fuellung: "ergebnis" });

export default s;
```

- [ ] **Step 2: Golden erzeugen und geänderte PNGs prüfen**

Run: `npm run update-golden`
Dann `git status --short tests/golden/` — nur die zwei neuen PNGs dürfen neu sein, die bestehenden byte-identisch.

- [ ] **Step 3: Beide Bilder ansehen (Controller, Read-Tool)**

`tests/golden/dreieck.png` und `tests/golden/dreieck-gefuellt.png` öffnen und prüfen: gleichseitig, Spitze oben; das obere Label mittig über der Spitze, die unteren links/rechts außen an den Ecken; nichts überschneidet den Umriss unschön; die Füllung wirkt dezent (nicht deckend). Bei Mängeln `AUSSEN`, Label-Positionen oder Füllfarbe anpassen, neu erzeugen, wieder ansehen.

- [ ] **Step 4: Golden-Test**

Run: `npx vitest run tests/golden-render.test.js`
Expected: PASS — die neuen Szenen byte-identisch, bestehende unverändert.

- [ ] **Step 5: Commit**

```bash
git add tests/golden/dreieck.mjs tests/golden/dreieck.png tests/golden/dreieck-gefuellt.mjs tests/golden/dreieck-gefuellt.png
git commit -m "feat: Golden-Referenz Dreieck (Umriss + gefüllt)"
```

---

## Abschluss

Nach Task 5:

- Das `line`-Primitiv kann optional füllen (`fuellung`); ungefüllte Linien und Golden unverändert.
- `dreieck` baut ein gleichseitiges Dreieck (Spitze oben) mit drei Ecken-Labels, optional getönt; validiert ohne Befund; in den Referenzen dokumentiert; per Golden (Umriss + gefüllt) visuell bestätigt.
- **Nächste Stufe-4-Zyklen:** Mengenkreise/Venn (nutzt die neue Füllung + reduzierte Deckkraft für Überschneidungen), Programmablaufplan; sowie SVG für `f.image`, die zwei Validator-Feinschliffe (Hermes-Feedback) und die Bereitstellung.
